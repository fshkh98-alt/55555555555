import {NextResponse} from 'next/server';import {requireAdmin} from '../../../../lib/auth';import {getDb} from '../../../../lib/prisma';
export async function POST(req:Request){if(!(await requireAdmin()))return NextResponse.json({error:'Unauthorized'},{status:401});const db=getDb();if(!db)return NextResponse.json({error:'DATABASE_URL is not configured'},{status:503});const b=await req.json();const data={siteName:b.siteName||'Ozaib',tagline:b.tagline||'Cybersecurity Student',description:b.description||null,defaultLanguage:b.defaultLanguage==='ar'?'ar':'en',accentColor:b.accentColor||'#38bdf8',analyticsEnabled:!!b.analyticsEnabled,maintenanceMode:!!b.maintenanceMode};const old=await db.siteSettings.findFirst();return NextResponse.json(old?await db.siteSettings.update({where:{id:old.id},data}):await db.siteSettings.create({data}))}
