import {NextResponse} from 'next/server';import {requireAdmin} from '../../../../lib/auth';import {getDb} from '../../../../lib/prisma';
export async function POST(req:Request){if(!(await requireAdmin()))return NextResponse.json({error:'Unauthorized'},{status:401});const db=getDb();if(!db)return NextResponse.json({error:'DATABASE_URL is not configured'},{status:503});const b=await req.json();const data={displayName:b.displayName||'Ozaib',title:b.title||'Cybersecurity Student',shortBio:b.shortBio||null,longBio:b.longBio||null,currentFocus:b.currentFocus||null};const old=await db.profile.findFirst();return NextResponse.json(old?await db.profile.update({where:{id:old.id},data}):await db.profile.create({data}))}
