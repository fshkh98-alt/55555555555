"use client";
import { useState } from "react";
import { AdminLayout } from "./admin-overview";
import { AdminForm, Field, Check } from "./admin-page";
import { SocialManager } from "./resource-manager";
export default function ProfileEditor({profile,links}:{profile:any;links:any[]}){const [msg,setMsg]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg("");const data=Object.fromEntries(new FormData(e.currentTarget).entries());try{const r=await fetch(`/api/admin/profile`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(!r.ok)throw new Error("Could not save profile");setMsg("Profile saved.")}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}return <AdminLayout active="Profile & Links"><div className="space-y-8"><div className="premium-card"><h2 className="mb-5 text-xl font-semibold">Profile</h2><AdminForm onSubmit={submit} busy={busy} submitLabel="Save profile"><div className="grid gap-4 md:grid-cols-2"><Field label="Display name" name="displayName" defaultValue={profile?.displayName||""}/><Field label="Title" name="title" defaultValue={profile?.title||""}/><Field label="Short bio" name="shortBio" defaultValue={profile?.shortBio||""}/><Field label="Current focus" name="currentFocus" defaultValue={profile?.currentFocus||""}/><div className="md:col-span-2"><Field label="Long bio" name="longBio" defaultValue={profile?.longBio||""} multiple/></div></div>{msg&&<p className="text-sm text-sky-500">{msg}</p>}</AdminForm></div><SocialManager items={links}/></div></AdminLayout>}
