"use client";
import { useState } from "react";
import { AdminLayout } from "./admin-overview";
import { AdminForm, Field, Select, Check } from "./admin-page";
export default function SettingsEditor({settings}:{settings:any}){const [msg,setMsg]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg("");const f=new FormData(e.currentTarget);const data:any=Object.fromEntries(f.entries());data.analyticsEnabled=f.get("analyticsEnabled")==="on";data.maintenanceMode=f.get("maintenanceMode")==="on";try{const r=await fetch('/api/admin/siteSettings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error('Could not save settings');setMsg('Settings saved.')}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}return <AdminLayout active="Settings"><div className="premium-card max-w-3xl"><h2 className="text-xl font-semibold">Site settings</h2><p className="mt-2 mb-6 text-sm text-zinc-500">Global identity and privacy controls. Analytics stays off unless you explicitly enable it.</p><AdminForm onSubmit={submit} busy={busy} submitLabel="Save settings"><div className="grid gap-4 md:grid-cols-2"><Field label="Site name" name="siteName" defaultValue={settings?.siteName||"Ozaib"}/><Field label="Tagline" name="tagline" defaultValue={settings?.tagline||"Cybersecurity Student"}/><Select label="Default language" name="defaultLanguage" defaultValue={settings?.defaultLanguage||"en"} options={[["en","English"],["ar","Arabic"]]}/><Field label="Accent color" name="accentColor" defaultValue={settings?.accentColor||"#38bdf8"}/><div className="md:col-span-2"><Field label="Site description" name="description" defaultValue={settings?.description||""} multiple/></div><div className="flex gap-6 md:col-span-2"><Check label="Enable private analytics" name="analyticsEnabled" defaultChecked={settings?.analyticsEnabled}/><Check label="Maintenance mode" name="maintenanceMode" defaultChecked={settings?.maintenanceMode}/></div></div>{msg&&<p className="text-sm text-sky-500">{msg}</p>}</AdminForm></div></AdminLayout>}
